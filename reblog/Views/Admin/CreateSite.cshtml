@model reblog.App.Domain.Post

<style type="text/css">
    .input_group {
        max-width: 200px;
        min-width: 100px;
    }
</style>

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            @using (Html.BeginForm("CreateSite", "Admin", FormMethod.Post, new {}))
            {
                @Html.ValidationSummary(true)

                <fieldset>
                    <legend>Nova categoria</legend>

                    <div class="control-group">
                        <label class="control-label" for="tag">Sites existentes</label>
                        <div class="controls">
                            <select name="existingtags" id="tag">
                                @foreach (reblog.App.Domain.Owner owner in @ViewBag.Owners)
                                {
                                    <option value="@owner.Id">@owner.Name</option>
                                }
                            </select>
                        </div>
                    </div>

                    <div class="control-group">
                        <label class="control-label" for="owner">Nome</label>
                        <div class="controls">
                            <input type="text" name="owner.Name" id="owner" class="form-control input-xlarge" />
                        </div>
                    </div>

                    <p>
                        <input type="submit" value="Create" />
                    </p>
                </fieldset>
            }
        </div>
    </div>
    <div class="row">
        @Html.ActionLink("Voltar", "Index")
    </div>
</div>



